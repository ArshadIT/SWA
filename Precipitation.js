// precipitation

function precipitation(tupe, inn, mm){

    const precipitationType = () => type;
    const convertToInches = () => mm;
    const convertToMM = () => inn;
    return {
        precipitationType,
        convertToInches,
        convertToMM
    }
}